{% extends 'base.html.twig' %}

{% block title %}Administrer Profil{% endblock %}

{% block body %}

    <div class="container">
    <h1>Modifier un profil</h1>

    {{ form_start(form) }}
    {{ form_widget(form) }}
    <button type="submit">Modifier le profil</button>
    {{ form_end(form) }}
    </div>

{% endblock %}
