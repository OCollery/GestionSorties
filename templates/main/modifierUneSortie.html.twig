{% extends 'base.html.twig' %}

{% block title %} Modifier sortie | {{ parent() }}{% endblock %}

{% block main %}

<h1>Modifier une sortie</h1>

{{ form_start(updateForm) }}
    {{ form_widget(updateForm) }}
{{ form_end(updateForm) }}

{% endblock %}